from django.contrib.auth.models import AbstractUser

#importando uma classe pronta do django, se for necessário adicionar algum atributo a ela é só escrever nesta classe
class User(AbstractUser):
    pass