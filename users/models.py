from django.contrib.auth.models import AbstractUser

#importando uma classe pronta do django, se for necessário adicionar alguma coisa a ela é só escrever nesta classe(métodos, atributos, etc).
class User(AbstractUser):
    pass